class Solution {
    static int findTheWinner(int n, int k) {
       return helper(n,k)+1;
   }
    static int helper(int size, int k){
        if(size==1)return 0;
        int c= (helper(size-1,k)+k)%size;
        return c;
    }
}
